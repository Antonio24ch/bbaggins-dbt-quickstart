version: 2

seeds:
  - name: employees
    description: a manual map op employees to customers
    columns:
      - name: employe_id
        tests:
          - unique
          - not_null
              